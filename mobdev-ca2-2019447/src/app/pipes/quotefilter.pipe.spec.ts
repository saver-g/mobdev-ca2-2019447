import { QuotefilterPipe } from './quotefilter.pipe';

describe('QuotefilterPipe', () => {
  it('create an instance', () => {
    const pipe = new QuotefilterPipe();
    expect(pipe).toBeTruthy();
  });
});
